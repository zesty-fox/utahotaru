# Changelog

## [1.1.2] - 2026-06-05

### 新增功能
- *（待补充）*

### 特性改变
- 「软件导出补偿」修改改走静默保存通道，不再触发整条 settings cascade（该值只在导出/导入时被消费）

### 修复项目
- 从预设加载演唱者时，若项目仅有占位默认演唱者，其名下歌词会被级联删除
- 设置 cascade 加防御性兜底：`_apply_settings` 与全局偏移更新中的 Python 异常不再透传到 Qt 派发层，避免出现 0xC0000409 原生闪退
- `build.py` 强制把本地 `src/` 顶到 `sys.path` 最前，避免环境里旧位置的 `pip install -e .`（如 `E:\KaraMaker\StrangeUtaGame\`）让 PyInstaller 把旧 bytecode 打进 PYZ 导致打包后修复失效，同时打印实际命中的 `strange_uta_game` 路径便于自检


## [1.1.1] - 2026-06-04

### 特性改变
- 从预设加载演唱者时，自动替换占位用未命名默认演唱者

### 修复项目
- 自动补全时间戳未检测半角字符
- 中文歌曲检测异常


## [1.1.0] - 2026-06-03

### 新增功能
- 支持使用LLM注音
  - 不提供API，请自行配置已有的API
  - 使用LLM注音时跳过本地注音环节（建议不应用用户词典）
  - LLM注音效果视使用的模型效果有变化
    - 测试时gpt-5.4-mini>mimo2.5-pro≈deepseek-v4-flash>gpt-5.4-nano
  - LLM注音失败时回退普通注音
- 新增快捷键，打轴并删除下一节奏点（默认无快捷键）
  - 普通节奏点与句尾节奏点一视同仁
- nicokara导出时可以启用每行行首处插入标签
- 新增版本noWinRT(请前往github-release下载变体)
  - 采用sudachimini注音，注音效果与WinRT有出入+110M运行环境
- 新增分离符号功能（打轴界面-时间戳工具-分离符号时间戳）
  - 具体处理范围见弹窗顶部说明，注cc=CheckCount=普通节奏点

### 特性改变
- 自动补偿时间戳/调整原始时间戳整合入时间戳工具
- 自动补偿时间戳自动过滤句尾无节奏点空格

### 修复项目
- 部分场景下karaoke预览错误


## [1.0.7] - 2026-05-31

### 新增功能
- 插入导唱符提供按时间自动填满功能
- 主窗口会记忆上次调整过的大小
- nicokara标签和分色助手允许滚动（避免分色过多时无法确认）

### 修复项目
- 部分场景下注音异常
- 部分场景下走字异常


## [1.0.6] - 2026-05-31

### 新增功能
- 卡拉OK预览界面可设置
  - 设置-界面-两种字体
- 行编辑页面的字宽统计根据设置的主文字字体统计
- F2/修改所选字符/批量编辑界面新增快速连词功能

### 特性改变
- F3在多选的时候可以一键连词/取消连词
- 重构按行编辑界面后台算法
  - 字符按照项目内格式解析
  - 删除一些遗留的错误规则

### 修复项目
- 修复了主文字与ruby间隔设置从未生效


## [1.0.5] - 2026-05-30

### 新增功能
- 新增按键音功能（开启后会影响打轴习惯,所以默认关闭）
  - 开关位置：设置-打轴-按键音
  - 可选四种风格、建议使用200%音量
  - 打开后很像音游,但是需要预判打轴了（）
- N3格式的LRC导入后的解析（保留原有注音）更准确了

### 特性改变
- 「ん/ン」check 开关会影响ruby上的ん/ン，但是不会影响用户字典
- 调整F2注册词典的位置与其他修改字符窗口一致
- 尝试优化了F3的操作手感

### 修复项目
- 修复行间距系数为0时闪退
- 修复ruby组走字退化为匀速
- 修复逗号无法被设置为快捷键


## [1.0.4] - 2026-05-30

### 新增功能
- 波形显示界面现在会标注发生时间倒退的时间戳
- 新增演唱者时可以使用项目内的分组和颜色
- 新增控件允许批量调整原始时间戳

### 特性改变
- 隐藏无用的演唱者编号
- 纯空格无时间戳空行导出时会过滤空格
- 现在可以正确的选中纯空行了

### 修复项目
- 部分场景下罗马音标注错误
- 初次打开软件时滚动状态切换按钮未着色


## [1.0.3] - 2026-05-29

### 新增功能
- 支持朴素罗马音转换，在设置-AutoCheck中打开@FMPeach/键盘office
- 支持波形隐藏按钮
- 支持Karaoke预览界面滚动条

### 特性改变
- 全文本编辑界面当软件全屏时自动全屏
- 滚动状态切换按钮着色
- 自动更新时备份原文件
- 检测自动更新时获取所有已跳过版本的更新日志
- 隐藏无用的应用设置按钮

### 修复项目
- 音频播放结束后再播放各控件位置未刷新
- 自动补偿时间戳未识别到自身的句尾时间戳
- 连词组中含有节奏点的字符解析异常
- 新建演唱者时分色无法使用
- 从sug直接打开软件时未加载新sug信息。


## [1.0.2] - 2026-05-28

### 新增功能
- 支持ASS格式导出输出行内演唱者标签
  - Name更改为所有行内演唱者拼接，如单行内有Shiro,Sora两位演唱者，实际name字段为Shiro_Sora
  - 行内演唱者切换出添加{\sing_人名标签}，还是上方的例子，实际输出会为{\k0}{\sing_Shiro}{\k12}ni{\k26}hao{\sing_Sora}{\k11}shi{\k21}jie{\k0}
- 支持演唱者分色，最多五色
- 新增快捷键，编辑模式下应用时间戳。（默认快捷键为空，请自行设置）
- 新增快捷键，一键清除所有节奏点。（默认快捷键为空）
- 新增中文歌词识别，无日文假名时自动按照中文歌词标注（支持开关）
  - 每个字符1个节奏点
  - 其他check规则与原有保持一致
- 新增滚动模式切换
  - 循环选择自动滚动/始终滚动/不滚动
  - 其中自动滚动用户操作后自动挂起6秒后继续自动滚动
- 新增标题栏显示当前项目名称与保存状态
- 以后nicokara标签与媒体路径会存储在.sug内
- 导唱符允许自动添加反向时间戳

### 特性改变
- 按字符均分修正更改为依据rubypart平分字符
- 按行应用演唱者功能自动跳转到当前焦点行
- 按行应用演唱者功能补足筛选器
- 从软件预设加载演唱者默认更改为全不选
- 大幅度放开了界面设置，字体允许0~99px。行距允许-1.0~5.0

### 修复项目
- 修复打轴时错误的跳字预览
- 修复加载文件路径时未能直接卸载导出界面
- 修复普通LRC时间为毫秒级别
- 部分场景下未清理临时文件
- 打包时arm64.dll丢失
- 修复导出时与打轴时的完成度计算不一致
- 换行时逻辑反常
- ruby连词框重叠问题

### Todo项目
- nicokaraLrc完整导入（目前解析不完整）
- 音频波形界面增强显示时间戳重叠部分
- 音频波形界面允许隐藏
- 部分界面增加一键到顶一键到底
- 罗马音注音
- LLM注音

## [1.0.1] - 2026-05-24

### 新增功能
- 在网络环境允许的条件下支持一键安装FFmpeg

### 修复项目
- 部分场景下走字异常
- 部分场景下按行设置演唱者异常


## [1.0.0] - 2026-05-23

### 首次正式发布版本 ###

### 新增功能
- ctrl+v支持项目内的时间戳格式
- 全文本编辑界面字宽可开关显示
- 全本文编辑界面信息记忆

### 特性改变
- 调整部分文字描述
- 拉取更新器时显示进度
- 更改了Check规则应用顺序和设置存储位置

### 修复项目
- 启动时强制刷新资源管理器
- 全文本编辑界面不再把异常结构化字符当作错误的结构化字符处理
- 更新器优先判断是增量还是全量再做下载
- 更新器下载性能优化
- 应用默认音量时不在会影响实际调整过的音量
- 应用默认音量时候也会同步音量把手。
- 修复部分场景下的两次确认是否不保存
- 修复新建项目/加载文件还未改动时就认为有改动。
- 修复部分场景下走字异常
- 修复部分场景下ruby框绘制异常


## [0.3.20] - 2026-05-23

### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### v0.3.13更新目前会阻断Ui进程，且失败后大概率引起未响应！
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！

### 新增功能
- 演唱者支持分组功能
  - 管理界面和导入界面可通过分组筛选
  - 管理界面可批量设置分组
  - 现在可以保存同名不同分组的演唱者
    - 注：项目内仍旧不允许重名
- 支持设置FFmpeg路径（设置-关于-工具配置）
- 行编辑界面右侧显示绘制时字符占用的ch
  (类似RL的标尺，已自动排除格式化字符)

### 特性改变
- 删除PyAV库调用，减小Runtime大小
- 注音分析更换为异步并显示进度条
- 优化波形条的操作手感
  - 缩放条改为对数尺，放大缩小更均匀
  - 可以按住波形拖动查看整个时间轴
  - 修复放大情况下的进度条整体异常
  - 更改波形头回调逻辑，操作后挂起，6S后恢复自动回调

### 修复项目
- 修复音乐播放时，切换到打轴界面会产生快速抖动


## [0.3.19] - 2026-05-22

### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### v0.3.13更新目前会阻断Ui进程，且失败后大概率引起未响应！
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！

### 特性改变
- 更换为RL同款音频引擎（建议参考RL重新设置音频/导出偏移）
- 速度实时切换，预渲染结束后更换为无爆音的音频
- 不需要高质量音频可以在 设置-演奏控制关闭
- 速度条更换为滑块（可自定义滑块范围默认0.2-1.0，最大0.2-2.0）
- QW调速步长更换为0.05
- 优化了红色光标（current域）的跳转逻辑，现在更加跟手了。

### 修复项目
- 连词组的ruby边框不再重叠了
- 优化了自动补偿轴点的算法
- 现在双击空轴点字符的时候正确向前跳转并移动轴点了
- 修复了F2未采用用户焦点字符


## [0.3.18] - 2026-05-21

### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### v0.3.13更新目前会阻断Ui进程，且失败后大概率引起未响应！
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### 新增功能
- 全文本编辑界面回归（默认快捷键ctrl+T打开，打轴界面按钮可打开）
  - 特殊功能alt+滚轮缩放字体大小
  - [时间]为普通时间戳在目标字符前
  - [>时间]为句尾时间戳在目标字符后
  - [T]、[>T]为未打轴
  - 注音块{原文||注音1,注音2}，块内即连词
  - 注音1内，采用单个|划分rubyPart
  - 如果句尾字符在注音块内例子1：{今日|[时间]きょ|[时间]う,[>时间]} 例子2：{時||[时间]と|[时间]き}{々||[时间]ど|[时间]き[>时间]}
- 新增局部分析划词注音（默认快捷键ctrl+A）
- 新增分析选中行注音（默认快捷键ctrl+shift+A）
- 可从候补字典中获取不同注音

### 特性改变
- 导入歌词时保留空行。（不含空格）
- 创建项目后的ctrl+v可以解析多行纯文本


## [0.3.17] - 2026-05-20

### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### v0.3.13更新目前会阻断Ui进程，且失败后大概率引起未响应！
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！

### 新增功能
- 支持ctrl+c/v功能

### 特性改变
- 移除了sudachi注音更换为WinRT注音
  (runtime体积减小220M但是需求安装Windows日文包)
- 优化内置词典
- 优化了连词框的显示

### 修复项目
- 打包环境下并未使用utf-8


## [0.3.16] - 2026-05-20
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### v0.3.13更新目前会阻断Ui进程，且失败后大概率引起未响应！
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！

### 新增功能
- 支持RL字典导入ini/txt
- 支持网络字典拉取，内置RL官方源/键盘office源（感谢维护！）
  首次使用请手动在**设置 - 读音词典 - 网络词典管理**刷新源，默认不拉取
- 支持编辑不同来源的字典优先级
- 支持启用自动更新网络源 - 默认关闭
- 支持设置网络源更新间隔 - 默认一周



## [0.3.15] - 2026-05-19

### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### v0.3.13更新目前会阻断Ui进程，且失败后大概率引起未响应！
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！

### 新增功能
- 引入pyphen用于区分引文单词音节（可开关默认开）
- 信息栏新增字数统计 

### 特性改变
- 恢复对特殊导唱符插入的支持

### 修复项目
- 打轴界面无法设置全局偏移
- 修复单字英文单词未被识别为英文单词（I,A等）
- 部分场景下导出时未给默认演唱者插入标签


## [0.3.14] - 2026-05-18

### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！
### 0.3.13更新有概率失败引起未响应
### 请确认能够连接GITHUB后再更新！！！
### 请确认能够连接GITHUB后再更新！！！

### 新增功能
- 更新内置词典

### 特性改变
- 剪贴板添加非nicokara格式也会自动注音了
- 更改部分默认快捷键设置
- 更改连词秒数
- 单击空CC字符时一定向前跳转
- 默认句尾轴点更新为圆形（注意一定要重置默认设置后生效！！）
  
### 修复项目
- 修复WIN11下的主题显示异常
- 自动补全时间戳使用方法更新
- 音频播放完后不能继续播放
- 自动更新，更新器阻塞UI导致未响应
- 设置项和打轴界面的按类型删除注音导致不一致


## [0.3.13] - 2026-05-18

### 新增功能
- 插入77条自定义字典
- 合并到上一行新增不可更改快捷键enter+shift
- 自动补全时间戳现在支持行首行尾边界字符的补偿

### 特性改变
- 双击CP跳转时不再跳到该字符的第一个CP
- 按类型删除注音中片假名拆分为两个子类型
- 打轴界面下方状态栏字体颜色更改为黑色，大小增加1px
- 双引号单引号引入标点识别
- 变速音频下zx快进快退,backspace快进快退以现实播放时间为准

### 修复项目
- 继续维护遗留的全局偏移未应用
- 默认演唱者异常强制导出


## [0.3.12] - 2026-05-17

### 新增功能
- 点击空白处会就近寻找字符
- 增加主程序更新更新器
- 更新器支持重试断点续传（获得此版本后）
- 右键菜单增强：1：在前方插入空格。2：在前方插入空行。

### 特性改变
- LUR音频缓存更改为5份
- 音频引擎向Windows申请的延时更换为100ms，避免低性能机器下卡顿
- 优化设置页面的UI
- 清理更换了一些github代理源

## [0.3.11] - 2026-05-16

### 特性改变
- 更新了哈希值的计算逻辑，继续尝试修复增量更新

### 修复项目
- 导出时默认演唱者标签到处异常
- 用户词典更新时系统环境语言异常导致报错
- 打轴完成度中空行不纳入统计
- 自动补偿Windows音频流创建导致的偏移


## [0.3.10] - 2026-05-16

### 新增功能
- 允许自动补轴点时单独选择ん/ン

### 特性改变
- 现在行编辑的时间戳和界面中的时间戳一样了
- 现在更新字典不会直接覆盖用户词典了
- 加载sug时会获取sug中的全局偏移

### 修复项目
- 修复了部分内置用户词典异常
- 修复了项目应用/创建新项目时字符未应用全局偏移


## [0.3.9] - 2026-05-16

### 特性改变
- 对ctrl+z/ctrl+y进行了重构，现在应该更符合预期了

### 修复项目
- 修复了老Updater对于全量更新不兼容


## [0.3.8] - 2026-05-16

### 新增功能
- *（待补充）*

### 特性改变
- *（待补充）*

### 修复项目
- 预设全局偏移为-390ms


## [0.3.7] - 2026-05-16

### 新增功能
- 无更新内容，纯测试自动更新用

### 特性改变
- *（待补充）*

### 修复项目
- 修复版本号显示


## [0.3.6] - 2026-05-16

### Added
- **什么都没改纯粹测试自动更新**

### Changed
- *（待补充）*

### Fixed
- *（待补充）*


> 本文件维护 StrangeUtaGame 的发布历史。
>
> **每发一版**：在文件顶部新增一段（推荐用 `scripts/release.py prepare X.Y.Z`
> 自动注入模板）；发布脚本会从这里抽取对应版本的段落作为 GitHub Release body，
> 该 body 会原样显示在用户的"有新版本"弹窗里。
>
> **写作格式**：遵循 [Keep a Changelog](https://keepachangelog.com/) 风格 +
> [SemVer](https://semver.org/lang/zh-CN/)。每段标题严格使用 `## [X.Y.Z] - YYYY-MM-DD`。



## [0.3.5] - 2026-05-16

### Added
- （在这里写未发布的新增内容…）

### Added
- **增量更新（分包式）**：发布产物在原有全量 `StrangeUtaGame-vX.Y.Z.zip` 之外
  额外生成 `-app.zip`（~5MB，含主 EXE + Updater.exe + 应用代码）、`-runtime.zip`
  （~178MB，含 PyQt6/numpy/pyav/sudachidict 等依赖）以及一份
  `manifest-vX.Y.Z.json` 描述各 part 的 sha256 与管辖目录。Updater 升级时优先
  拉取 manifest，比对本地 `_internal/.installed_manifest.json` 中的 part sha256，
  只下载哈希变化的 part 并精确替换。小版本升级（应用代码改动）通常只需要下载
  约 5MB，节省 ~95% 带宽。
  - **兼容策略**：找不到远端 manifest / 找不到本地 manifest / 任何 part 下载或
    应用失败 → 自动回退到现有的全量 zip 流程，行为与旧版完全一致。
  - **首次升级即可走增量**：构建阶段直接把出厂版本的
    `_internal/.installed_manifest.json` 打进全量 zip 一并发布，因此用户无论
    通过哪种方式拿到本版本（GitHub Web 直接下载解压、Updater 全量装、Updater
    增量装）都自带本地清单。下次升级 Updater 读到清单即可走增量路径，无需"必须
    先经历一次全量"。
  - **构建顺序**：part-zip 不包含 `.installed_manifest.json`（避免 sha256 循环
    依赖）；先打 part-zip 算 sha256，再用 sha256 写本地清单到 dist，最后打全量
    zip。全量 zip 因此天然带清单。
- **发布资产自动生成 `.zip.sha256` 校验文件**：`scripts/release.py build` 与
  GitHub Actions workflow 都会同时输出 `StrangeUtaGame-vX.Y.Z.zip.sha256`
  （sha256sum / coreutils 兼容格式），并随 Release 一并上传。Updater 拿到主
  zip 后会自动按 `<zip-url>.sha256` 拉取同源的校验文件做完整性校验；找不到
  就降级为"跳过校验"，与旧版本向后兼容。

### Changed
- 更新弹窗的 changelog 改为用 `QTextEdit.setMarkdown` 渲染 Markdown，
  支持 ###/列表/链接/`代码`/代码块等 GFM 主要语法。

### Fixed
- **`Updater.exe` 不会被重新打包 → 新功能（manifest/sha256/增量）永远不生效**：
  之前 `scripts/release.py` 的 `_ensure_updater_exe()` 只检查 `Updater.exe` 是否
  存在，不检查 `updater_app/main.py` 等源码是否更新。结果：第一次 release.py
  打出 Updater.exe 后，后续即便改了 Updater 代码、再跑 release.py，Updater.exe
  也**绝不**会被重打 —— 发布出去的 zip 里永远是历史上第一次打的旧 Updater。
  改为：比较 `updater_app/**/*.py` 的最大 mtime 与 `Updater.exe` 的 mtime，前者
  更新就强制重打。同时新增 `--rebuild-updater` CLI 选项做显式强制重打。
- **主程序在更新时不会真正退出 → Updater 备份 `_internal` 失败 (WinError 5)**：
  之前用 `QApplication.quit()` 退出，遇到脏项目数据、modal 弹窗、未结束的
  QThread 时不会真退出，导致 Updater 拿不到 `_internal` 写权限。改为新增
  `MainWindow.request_force_quit()` —— bypass "未保存"对话框，把脏数据兜底
  写到 `.cache` 临时文件，250ms 内 `os._exit(0)` 硬退出。
- **Updater 在 PID 消失后立刻动手 → 文件句柄未释放报 Access Denied**：
  Windows 内核释放 DLL/`_internal` 句柄是异步的，主进程 "退出" 后还会
  hold 文件锁一两秒。Updater 现在在 `wait_for_pid_exit` 后**再宽限 2 秒**，
  且对 `os.rename` / `shutil.copytree` 等关键操作做最多 6 次、每次 1.5s 的
  `PermissionError` 重试。
- **Updater.exe 启动后看不到任何输出**：之前用 `DETACHED_PROCESS` flag
  让 Updater 完全无控制台，所有 `print` / 报错都被吞掉。改为
  `CREATE_NEW_CONSOLE | CREATE_NEW_PROCESS_GROUP`，启动一个独立 cmd 窗口
  让用户看到下载进度与错误。
- **Updater.exe 报 `No module named 'colorsys'`**：PyInstaller 在
  `--exclude-module=qfluentwidgets/PyQt6` 的副作用下把一些标准库小模块也
  一并 exclude。给 `build_updater.py` 显式 `--hidden-import=colorsys` 以及
  `encodings.*`、`hashlib`、`zipfile`、`ssl` 等易漏标准库做兜底。
- **主程序 `StrangeUtaGame.exe` 也报 `No module named 'colorsys'`**：
  `backend/domain/entities.py` 直接 `import colorsys` 做颜色 HSV 变换，
  PyInstaller 这次静态分析没把它列入产物。给 `build.py` 显式声明
  `--hidden-import=colorsys` 兜底。
- **GitHub Actions 创建 Release 时 403 失败**：默认 `GITHUB_TOKEN` 只读，
  `softprops/action-gh-release` 无法创建 Release。
  解决：`.github/workflows/release.yml` 顶部声明 `permissions: contents: write`。
- **GitHub Actions Windows runner 的 UTF-8 编码问题**：runner 默认 stdout
  编码为 cp1252，会让发布脚本的中文 `print` 抛 `UnicodeEncodeError`。
  解决方案：`.github/workflows/release.yml` 顶部加 `PYTHONIOENCODING=utf-8` /
  `PYTHONUTF8=1`，并给 `build.py` / `build_updater.py` / `updater_app/main.py`
  / `scripts/release.py` 加 `_force_utf8_stdio()` 兜底。

## [0.3.4] - 2026-05-16

### Added
- * None *

### Changed
- * 更改sug数据结构，以后新保存的sug会记录全局偏移信息 *

### Fixed
- * None(用作自动更新测试用)*

## [0.3.3] - 2026-05-16

### Added
- **自动更新**：启动时检查 GitHub Release，发现新版本弹窗展示 changelog；用户
  确认后由独立 `Updater.exe` 替换 `StrangeUtaGame.exe` + `_internal/`，绝不
  触碰 `config.json` / `dictionary.json` / `singers.json` 等用户数据。
- **网络与代理设置**：支持「关闭 / 系统代理 / 自动检测 / 手动指定」四种模式；
  自动检测会扫描常用本地代理端口（含 Clash Verge 7897、V2RayN 10809 等 18 个端口）；
  提供「测试连通性」按钮直连 `api.github.com/zen` 验证。
- **更新源优先级**：默认 `GitHub Release → GHProxy → FastGit`，用户可在弹窗里
  拖动 / 上下移动调整；前一源失败时自动接力到下一源。
- **启动检查间隔**：默认 8 小时内不重复发起启动期检查（手动检查不受限）。
- **关于卡片**：新增「检查更新」按钮，版本号改为动态读取 `__version__`。

### Changed
- 版本号统一收敛到 `src/strange_uta_game/__version__.py`；设置-关于的版本文案
  不再硬编码。
- 内置 `config.json` 增加 `updater` 默认节点；首次启动若用户配置缺失会自动写入。

### Fixed
- *（首次发布该模块；暂无修复条目）*

### 0.3.2-0.3.3未发布内容
- 修复Karaoke渲染逐字走字不够精确的问题
- 修改默认导出偏移为0
- 修改普通轴点对齐策略为对齐字符左侧
- 修复播放时backspace蓝色光标没有跟随的问题
- 功能强化，现在应用演唱者可以直接双击应用
- 功能新增，现在可以给插入空格绑定快捷键了，默认为M
- 功能新增，autocheck，check规则可以取消长音符号的自动添加节奏点了。
- 修复导唱符时间校验，避免小于零的时间戳
- 功能新增，现在可以快速复制emoji参数到其他行
- 修复当前保存为预设会覆盖旧预设的问题。
- 修复插入导唱符对特殊emoji的解析异常，已过滤0xfe0e（变体选择符 VS15）
- 隐藏了行编辑界面，全文本编辑界面，在线查询界面
- 更新了可爱软件图标(GPT生成)
- 确保RubyTag中的停顿符删除
- 修复新建字符未应用全局偏移的问题
- 支持利用停顿符号，辅助英文词语整体掉落
- 新增开关拦截用户词典中的英文

## [0.3.2] - 之前的版本

- 详见 GitHub Release 历史
